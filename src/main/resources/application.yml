
spring:
  datasource:
    url: jdbc:mysql://10.220.0.109:3306/testdb?serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: jonathan
    password: dmove1122!
    # JPA ?? ??, datasource: ??? ?? ??? ??????.
    # JPA? ?????? ??
    jpa:
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      show-sql: true
      hibernate:
        ddl-auto: update
        # create : ??? ? ?? ?? ???
        # update : table? drop?? ?? ???? ?????? ??

logging:
  level:
    org.hibernate.SQL: debug


